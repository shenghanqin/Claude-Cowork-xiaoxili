# Electron 镜像源配置（使用淘宝镜像）
electron_mirror=https://npmmirror.com/mirrors/electron/

# npm 镜像源（可选，如果需要加速 npm 包下载）
registry=https://registry.npmmirror.com

# 允许 electron 和 better-sqlite3 运行构建脚本
enable-pre-post-scripts=true
